{
  "name": "@block-hash/database",
  "version": "1.0.0",
  "description": "Database layer — PostgreSQL (Prisma), ClickHouse, Redis",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "ts-node src/prisma/seed.ts"
  },
  "dependencies": {
    "@block-hash/common": "*",
    "@prisma/client": "^5.18.0",
    "@clickhouse/client": "^1.4.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0",
    "typescript": "^5.5.0"
  }
}
